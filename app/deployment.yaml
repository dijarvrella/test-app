apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
  labels:
    app: app-team-a
    app.kubernetes.io/component: web
    app.kubernetes.io/instance: app-team-a
    app.kubernetes.io/name: app-team-a
    app.kubernetes.io/part-of: app-team-a
    app.openshift.io/runtime: java
  name: app-team-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-team-a
  template:
    metadata:
      labels:
        app: app-team-a
    spec:
      containers:
      - name: app-team-a
        imagePullPolicy: Always
        image: quay.io/service-binding/spring-petclinic:latest
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 45
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        ports:
        - containerPort: 8080
          protocol: TCP
        - containerPort: 8443
          protocol: TCP
        - containerPort: 8778
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 45
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 5
