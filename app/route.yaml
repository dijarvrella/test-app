apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: app-team-a
  name: app-team-a
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: app-team-a
    weight: 100
